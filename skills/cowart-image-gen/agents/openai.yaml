interface:
  display_name: "Cowart Image Gen"
  short_description: "Generate a Cowart image with selectable region metadata"
  icon_small: "./assets/imagegen-small.svg"
  icon_large: "./assets/imagegen.png"
  default_prompt: "Use $cowart:cowart-image-gen to generate an image into the selected Cowart AI image holder."

policy:
  allow_implicit_invocation: true
